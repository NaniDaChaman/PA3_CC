apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc
  namespace: team13
  labels:
    app: test-pvc
spec:
  storageClassName: default
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
